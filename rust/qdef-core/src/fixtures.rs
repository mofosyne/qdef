//! Stale fixtures from the old wire format — kept as reference but no
//! longer used by tests. The current tests use inline vectors (tests.rs).
//! Regenerate with `prototype/scripts/gen-rust-fixtures.js` once the
//! Node encoder matches this crate's grammar.
